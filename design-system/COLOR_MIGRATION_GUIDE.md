# 配色迁移指南 - 从绿色到科技蓝

> **目标：** 将所有绿色配色替换为科技蓝色系
> **日期：** 2026-02-19

---

## 📋 迁移概览

### 颜色映射表

| 类型 | 旧颜色 | 新颜色 | 使用场景 |
|------|--------|--------|---------|
| **CTA/主色** | `#22C55E` | `#3B82F6` | 按钮、链接、激活状态 |
| **悬停色** | `#16A34A` | `#60A5FA` | 悬停状态 |
| **激活背景** | `rgba(34, 197, 94, 0.15)` | `rgba(59, 130, 246, 0.15)` | 激活卡片背景 |
| **激活边框** | `rgba(34, 197, 94, 0.3)` | `rgba(59, 130, 246, 0.4)` | 激活边框 |
| **绿色光晕** | `rgba(34, 197, 94, 0.2)` | `rgba(59, 130, 246, 0.3)` | 阴影光晕 |
| **背景** | `#0F172A` | `#0B0B10` | 页面背景（更深） |

---

## 🔧 迁移步骤

### 步骤 1: 更新 globals.css

**需要更新的变量：**

```css
:root {
  /* 背景色（更深） */
  --background: #0B0B10;  /* 旧: #0F172A */
  --foreground: #F8FAFC;

  /* 主色调（绿色 → 蓝色） */
  --primary: #3B82F6;              /* 旧: #22C55E */
  --primary-hover: #60A5FA;
  --primary-active: #2563EB;

  /* 激活背景（绿色 → 蓝色） */
  --glass-bg-active: rgba(59, 130, 246, 0.15);  /* 旧: rgba(34, 197, 94, 0.15) */

  /* 激活边框（绿色 → 蓝色） */
  --glass-border-active: rgba(59, 130, 246, 0.4);  /* 旧: rgba(34, 197, 94, 0.3) */

  /* 阴影光晕（绿色 → 蓝色） */
  --glass-shadow-active: 0 4px 20px rgba(0, 0, 0, 0.25), 0 0 20px rgba(59, 130, 246, 0.3);
  /* 旧: 0 4px 20px rgba(0, 0, 0, 0.15), 0 0 20px rgba(34, 197, 94, 0.2) */

  /* 新增：蓝色光晕 */
  --glass-shadow-blue: 0 0 24px rgba(59, 130, 246, 0.25);
  --glass-shadow-blue-heavy: 0 0 32px rgba(59, 130, 246, 0.35);

  /* 深色背景（略微调整） */
  --glass-bg-dark: rgba(30, 41, 59, 0.6);  /* 旧: rgba(15, 23, 42, 0.6) */
  --glass-bg-dark-hover: rgba(30, 41, 59, 0.7);
  --glass-bg-dark-active: rgba(30, 41, 59, 0.8);

  /* 边框（略微调整） */
  --glass-border: rgba(148, 163, 184, 0.15);  /* 旧: rgba(255, 255, 255, 0.1) */
  --glass-border-hover: rgba(148, 163, 184, 0.25);
}
```

### 步骤 2: 批量替换硬编码颜色

**绿色 → 蓝色替换：**

```bash
# 主色替换
find src -type f \( -name "*.tsx" -o -name "*.ts" -o -name "*.jsx" -o -name "*.js" \) -exec sed -i '' \
  's/#22C55E/#3B82F6/g' {} +

# 悬停色替换（如果有）
find src -type f \( -name "*.tsx" -o -name "*.ts" -o -name "*.jsx" -o -name "*.js" \) -exec sed -i '' \
  's/#16A34A/#60A5FA/g' {} +

# rgba 绿色 → 蓝色替换
find src -type f \( -name "*.tsx" -o -name "*.ts" -o -name "*.jsx" -o -name "*.js" \) -exec sed -i '' \
  's/rgba(34, 197, 94, /rgba(59, 130, 246, /g' {} +

# 背景色替换（更深）
find src -type f \( -name "*.tsx" -o -name "*.ts" -o -name "*.jsx" -o -name "*.js" \) -exec sed -i '' \
  's/#0F172A/#0B0B10/g' {} +
```

### 步骤 3: 检查组件文件

**重点检查这些文件：**

1. **`src/app/page.tsx`** - 首页
   - 查找 `#22C55E` 替换为 `#3B82F6`
   - 查找 `color="#22C55E"` 替换为 `color="#3B82F6"`

2. **`src/app/auth/signin/page.tsx`** - 登录页
   - Brain 图标颜色

3. **`src/features/analysis/components/`** - 分析相关组件
   - 所有使用绿色的地方

4. **`src/components/shared/`** - 共享组件
   - 特别是按钮、徽章等

### 步骤 4: 更新文字颜色

**确保所有深色背景上使用亮色文字：**

```tsx
// ❌ 错误：深色背景 + 深色文字
<Box sx={{ bgcolor: 'var(--glass-bg-dark)', color: '#1E293B' }}>

// ✅ 正确：深色背景 + 亮色文字
<Box sx={{ bgcolor: 'var(--glass-bg-dark)', color: 'var(--glass-text-white-heavy)' }}>
```

**需要替换的文字颜色：**

```bash
# 在 glassmorphism 卡片内
color: '#1E293B' → color: 'var(--glass-text-white-heavy)'
color: '#334155' → color: 'var(--glass-text-white-medium)'
color: '#64748B' → color: 'var(--glass-text-white-light)'
```

---

## 🎯 逐文件检查清单

### P0 - 核心页面

- [ ] `src/app/page.tsx` - 首页
  - [ ] 图标颜色 (#22C55E → #3B82F6)
  - [ ] 文字对比度

- [ ] `src/app/analysis/page.tsx` - 分析页
  - [ ] 主题色
  - [ ] 文字颜色

- [ ] `src/app/auth/signin/page.tsx` - 登录页
  - [ ] 图标颜色
  - [ ] 按钮颜色

### P1 - 核心组件

- [ ] `src/features/analysis/components/ImageUploader/ImageUploader.tsx`
  - [ ] 激活状态背景
  - [ ] 激活边框

- [ ] `src/features/analysis/components/BatchUploader/BatchUploader.tsx`
  - [ ] 所有绿色引用

- [ ] `src/features/analysis/components/AnalysisResult/AnalysisCard.tsx`
  - [ ] 主题色

- [ ] `src/features/auth/components/SignInButton/index.tsx`
  - [ ] 按钮背景
  - [ ] 悬停效果

- [ ] `src/components/shared/Header/Header.tsx`
  - [ ] Logo 颜色（如果使用绿色）

### P2 - 其他组件

- [ ] `src/features/analysis/components/ProgressDisplay/` - 所有进度组件
  - [ ] 进度条颜色
  - [ ] 激活状态

- [ ] `src/features/analysis/components/ValidationStatus/index.tsx`
  - [ ] 状态指示器

- [ ] `src/components/shared/AITransparency/AITransparencyBadge.tsx`
  - [ ] 徽章背景色

---

## 🧪 测试验证

### 视觉测试

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **检查页面**
   - [ ] http://localhost:3000/ - 首页
   - [ ] http://localhost:3000/analysis - 分析页
   - [ ] http://localhost:3000/auth/signin - 登录页

3. **验证项目**
   - [ ] 所有绿色已替换为蓝色
   - [ ] 文字在深色背景上清晰可读
   - [ ] 激活状态使用蓝色
   - [ ] 悬停效果流畅
   - [ ] 阴影光晕为蓝色

### 对比度测试

**使用浏览器开发工具：**

1. 打开 Chrome DevTools
2. 选择元素
3. 查看 Contrast Ratio
4. 确保所有文字对比度 ≥ 4.5:1

**重点检查：**

- [ ] 主标题文字（白色 on 深色背景）
- [ ] 次要文字（银灰色 on 深色背景）
- [ ] 链接文字（蓝色 on 深色背景）
- [ ] 按钮文字（白色 on 蓝色背景）

---

## 🔄 回滚方案

如果迁移后发现问题，可以快速回滚：

```bash
# 回滚 globals.css（从备份恢复）
cp src/app/globals.css.bak3 src/app/globals.css

# 回滚组件文件（反向替换）
find src -type f \( -name "*.tsx" -o -name "*.ts" \) -exec sed -i '' \
  's/#3B82F6/#22C55E/g' {} +

find src -type f \( -name "*.tsx" -o -name "*.ts" \) -exec sed -i '' \
  's/rgba(59, 130, 246, /rgba(34, 197, 94, /g' {} +
```

---

## 📊 迁移统计

### 预期改动

| 类型 | 数量 |
|------|------|
| **CSS 变量更新** | ~15 个 |
| **组件文件修改** | ~20 个 |
| **颜色替换** | ~60 处 |
| **文字对比度修复** | ~10 处 |

### 完成标准

- ✅ 所有绿色已替换为蓝色
- ✅ 文字对比度全部 ≥ 4.5:1
- ✅ 无控制台错误
- ✅ 视觉测试通过
- ✅ 无障碍测试通过

---

## 🚀 执行迁移

准备好后，按以下顺序执行：

1. **备份当前状态**
   ```bash
   cp src/app/globals.css src/app/globals.css.bak3
   ```

2. **更新 globals.css** - 应用新的 CSS 变量

3. **批量替换颜色** - 运行 sed 命令

4. **手动检查组件** - 逐个验证

5. **测试验证** - 视觉 + 对比度

6. **提交更改**

---

**准备开始迁移？** 让我知道是否执行！
